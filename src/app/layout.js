export const metadata = {
    "title": "Letter Scatter Generator"
}

export default function RootLayout({ children }) {
    return (
        <html>
            <body>
                {children}
            </body>
        </html>
    );
}